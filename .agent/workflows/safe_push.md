---
description: Build the frontend locally and push changes if successful
---

1. Navigate to the frontend directory
2. Run the build command
3. If the build succeeds, push changes to the repository

```bash
cd frontend
npm run build
```

// turbo
```bash
git push origin main
```
